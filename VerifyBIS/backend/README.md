# Backend placeholder

This folder is intentionally empty of backend implementation.

Frontend API service is in `src/services/api.js`.

Expected endpoints:
- GET `/api/dashboard`
- POST `/api/tenders/analyze` (multipart field: `file`)

Set `VITE_API_BASE_URL` when the backend is available.
